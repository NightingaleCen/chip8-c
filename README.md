# CHIP-8 Emulator

A minimal CHIP-8 emulator written in C23 with SDL3.

## Build & Run

```sh
# Requires SDL3 (pkg-config)
make
make run ROMS/your-rom.ch8
```

Or directly: `./build/bin/chip8 romfile.ch8`

Press `Esc` to quit.
